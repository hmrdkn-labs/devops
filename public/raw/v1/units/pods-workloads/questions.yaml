schema_version: 1
unit_id: fpp:pods-workloads
revision: 4
questions:
  - id: fpp:pods-workloads/q-daemonset
    kind: scenario
    prompt: You need one log collector on every eligible worker node, including nodes added later. Which controller fits and why?
    model_answer: A DaemonSet. Its controller creates a matching Pod for each eligible node and responds as nodes join, leave, or change eligibility.
    critical_points:
      - Desired count derives from eligible nodes.
      - The use case is node-local infrastructure.
    objective_ids: [fpp:pods-workloads/controller-choice]
  - id: fpp:pods-workloads/q-share
    kind: explain
    prompt: What do two normal containers in the same Pod share, and what remains separate?
    model_answer: They share the Pod network namespace, IP, localhost, lifecycle placement, and declared shared volumes. They retain separate process identities, image filesystems, and resource settings unless explicitly configured otherwise.
    critical_points:
      - One Pod has one network identity.
      - Containers remain separate runtime processes.
    objective_ids: [fpp:pods-workloads/pod-boundary]
  - id: fpp:pods-workloads/q-practice-transfer
    kind: scenario
    prompt: "A nightly import must terminate after processing its input. Its importer and local validator communicate on localhost:9000 and exchange files in a shared volume. A teammate proposes a two-replica Deployment with one container per replica. What lifecycle and sharing mistakes would that introduce, and what design fits?"
    model_answer: "Use a CronJob to schedule a Job for finite work, with the coupled importer and validator in the same Pod and an explicitly shared volume. Separate replicas have separate Pod network namespaces and do not share localhost or container filesystems. A Deployment maintains long-running replicas rather than treating successful exit as job completion. Inspect CronJob-owned Jobs, their completion status, Pod container definitions and volume mounts; successful job status still needs an import-result check."
    critical_points:
      - "CronJob creates scheduled Jobs and Job tracks finite completion."
      - "Same-Pod localhost and explicit shared volumes do not extend across replicas."
    objective_ids: [fpp:pods-workloads/pod-boundary, fpp:pods-workloads/controller-choice]

  - id: fpp:pods-workloads/q-controller-classification
    kind: scenario
    prompt: "Match these needs to workload controllers: a stateless HTTP API, a database needing stable identity, one node-local agent per worker, and a backup that runs every night."
    model_answer: Use a Deployment for the stateless API, a StatefulSet for stable ordered identity and storage relationships, a DaemonSet for one eligible Pod per node, and a CronJob for scheduled finite work.
    critical_points:
      - Controller choice follows lifecycle, identity, and placement semantics.
      - DaemonSet and CronJob desired counts are not ordinary replica counts.
    objective_ids: [fpp:pods-workloads/controller-choice]
  - id: fpp:pods-workloads/q-localhost
    kind: predict
    prompt: Two containers run in the same Pod. Can one normally reach the other's listening port through localhost, and does that mean they automatically share the same filesystem?
    model_answer: They normally share the Pod network namespace and can communicate through localhost. Their container filesystems remain separate; data is shared only through explicitly mounted shared volumes or another deliberate mechanism.
    critical_points:
      - Containers in one Pod share network identity.
      - Filesystem sharing requires an explicit shared volume.
    objective_ids: [fpp:pods-workloads/pod-boundary]
