schema_version: 1
unit_id: fpp:cloud-native-delivery-gitops
revision: 4
questions:
  - id: fpp:cloud-native-delivery-gitops/q-ci
    kind: explain
    prompt: A CI job successfully builds and pushes image digest sha256:abc. What does that prove, and what does it not prove about production?
    model_answer: It proves the pipeline produced that artifact and completed its configured checks. It does not prove production desired state references the digest, the delivery controller applied it, Kubernetes completed a rollout, or users can successfully reach the new version.
    critical_points:
      - Artifact creation is distinct from environment deployment.
      - Runtime proof requires reconciliation and service-level evidence.
    objective_ids: [fpp:cloud-native-delivery-gitops/pipeline]
  - id: fpp:cloud-native-delivery-gitops/q-drift
    kind: scenario
    prompt: A GitOps-managed Deployment is manually edited in the cluster and then changes back a minute later. What is the most likely explanation?
    model_answer: The GitOps reconciler detected drift between live state and the version-controlled desired state and reconciled the cluster back to the declared version.
    critical_points:
      - Git contains desired state while the GitOps controller performs reconciliation.
      - Manual live edits may be temporary under continuous reconciliation.
    objective_ids: [fpp:cloud-native-delivery-gitops/gitops]
  - id: fpp:cloud-native-delivery-gitops/q-practice-transfer
    kind: scenario
    prompt: "CI built image digest D2, but production Git revision R10 still declares D1. The GitOps reconciler reports R10 synchronized and production returns version D1. A teammate calls this a failed Kubernetes rollout and wants a Git revert. Which boundary has not changed, and what would a revert actually affect?"
    model_answer: "Artifact creation succeeded, while production desired state still intentionally references D1; there is no evidence that Kubernetes was asked to roll out D2. Synchronization to R10 agrees with the D1 response. Inspect the environment manifest and reconciler's applied revision before diagnosing rollout. A reviewed promotion referencing D2 would change desired state; reverting a deployment commit changes that intent but does not reverse database writes or other external side effects."
    critical_points:
      - "Built artifact, environment desired revision and synchronized runtime are separate identities."
      - "A Git revert changes desired configuration, not external data history."
    objective_ids: [fpp:cloud-native-delivery-gitops/pipeline, fpp:cloud-native-delivery-gitops/gitops]
  - id: fpp:cloud-native-delivery-gitops/q-push-pull
    kind: explain
    prompt: A CI job calls kubectl apply with production credentials. Another design has an in-cluster agent pull the reviewed manifest repository continuously. What changes about deployment ownership and credentials, and why is Git alone insufficient to call the first design GitOps?
    model_answer: The first pushes a deployment from an external job; the second lets the target-side agent pull and reconcile desired state. Production write access can move from CI to the agent, but permissions and repository credentials still need protection. Git records configuration in either design; declarative/versioned intent plus automatic pulling and continuous reconciliation establishes the GitOps operating contract, not one apply command.
    critical_points:
      - Push/pull describes where deployment/reconciliation is initiated.
      - Credential location changes; credentials do not disappear.
    objective_ids: [fpp:cloud-native-delivery-gitops/gitops]
  - id: fpp:cloud-native-delivery-gitops/q-argocd-status
    kind: scenario
    prompt: Argo CD reports an Application Synced to the intended revision, but its Deployment is unhealthy and checkout fails. A teammate also assumes automated sync always prunes removed resources and reverses all manual edits. Which conclusions are supported and which policies should you inspect?
    model_answer: Sync identifies agreement with desired source; it is not a guarantee of workload health or successful user transactions. Inspect Deployment readiness/events and the failing request. Deletion of removed desired resources depends on pruning policy; manual drift correction depends on self-heal policy, which is separate from automated sync. Review the diff and deletion implications before enabling pruning in an authorized environment.
    critical_points:
      - Sync status and live health/user outcomes are different evidence.
      - Automated sync, pruning and self-healing are independently configured behaviors.
    objective_ids: [fpp:cloud-native-delivery-gitops/gitops, fpp:cloud-native-delivery-gitops/pipeline]
