schema_version: 1
unit_id: fpp:cloud-native-release-debugging
revision: 4
visuals:
  - id: fpp:cloud-native-release-debugging/visual-release-proof
    kind: evidence
    eyebrow: Release proof
    title: Trace a release all the way to real traffic
    lines:
      - "v2 Pod Running, Ready=false → slice entry can exist with ready=false"
      - "ordinary Service eligibility excludes unready v2; inspect probe/listener first"
      - "one v2 / ten Pods is not guaranteed 10% request allocation"
      - "routing executor + observed version traffic + abort criterion define canary"
    teaching_point: A successful rollout is only an intermediate proof; debugging continues through readiness, endpoints, routing, and an actual request.
